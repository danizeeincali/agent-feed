#!/usr/bin/env node
/**
 * NLD SSE Buffer Storm Deployment Script
 *
 * Deploys comprehensive monitoring for SSE buffer accumulation storms
 * Integrates with existing SSE infrastructure for real-time pattern detection
 */
declare class SSEBufferStormNLDDeployment {
    private deploymentStatus;
    constructor();
    /**
     * Deploy complete NLD monitoring system for SSE buffer storms
     */
    deployComplete(): Promise<void>;
    private initializeStorage;
    private startDetectors;
    private hookIntoSSEInfrastructure;
    private deploySSEInterceptors;
    private exportNeuralTrainingData;
    private validateDeployment;
    private reportDeploymentStatus;
    /**
     * Test the deployed system with simulated SSE buffer storm
     */
    simulateBufferStormTest(): void;
}
export { SSEBufferStormNLDDeployment };
//# sourceMappingURL=deploy-sse-buffer-storm-nld.d.ts.map