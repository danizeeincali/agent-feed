import { test, expect } from '@playwright/test';
import { AviChatPage } from '../../avi-dm/page-objects/AviChatPage';
import { PostingInterfacePage } from '../../avi-dm/page-objects/PostingInterfacePage';
import { TestHelpers } from '../../avi-dm/utils/test-helpers';
import { testMessages, mockResponses } from '../../avi-dm/fixtures/test-data';

test.describe('Concurrent Conversations', () => {
  test('should handle multiple browser windows chatting simultaneously', async ({ browser }) => {
    const contexts = await TestHelpers.createMultipleContexts(browser, 3);
    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));

    // Setup chat pages for each context
    const chatPages = pages.map(page => new AviChatPage(page));
    const postingPages = pages.map(page => new PostingInterfacePage(page));

    // Setup mock API for all pages
    for (const page of pages) {
      await TestHelpers.setupMockAPI(page, 'success');
    }

    // Navigate all pages to chat
    await Promise.all(postingPages.map(async (postingPage, index) => {
      await postingPage.navigateToPostingInterface();
      await postingPage.switchToTab('avi');
    }));

    // Send messages concurrently from all windows
    const messages = [
      'Hello from window 1',
      'Hello from window 2',
      'Hello from window 3'
    ];

    await TestHelpers.runConcurrentActions([\n      () => chatPages[0].sendMessage(messages[0]),\n      () => chatPages[1].sendMessage(messages[1]),\n      () => chatPages[2].sendMessage(messages[2])\n    ]);\n    \n    // Verify all messages sent successfully\n    await Promise.all(chatPages.map(async (chatPage, index) => {\n      await chatPage.waitForResponse();\n      await expect(chatPage.messagesContainer).toContainText(messages[index]);\n    }));\n    \n    // Verify each window maintains independent conversation state\n    for (let i = 0; i < chatPages.length; i++) {\n      const messageCount = await chatPages[i].getMessageCount();\n      expect(messageCount).toBe(2); // User message + response\n      \n      // Verify other window messages don't appear\n      for (let j = 0; j < messages.length; j++) {\n        if (i !== j) {\n          await expect(chatPages[i].messagesContainer).not.toContainText(messages[j]);\n        }\n      }\n    }\n    \n    // Cleanup\n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should handle rapid message exchanges in multiple windows', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 2);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Setup mock API\n    for (const page of pages) {\n      await TestHelpers.setupMockAPI(page, 'success');\n    }\n    \n    // Navigate to chat\n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Rapid exchange simulation\n    const rapidMessages = [\n      ['Quick question 1', 'Follow up 1', 'Final question 1'],\n      ['Quick question 2', 'Follow up 2', 'Final question 2']\n    ];\n    \n    // Send rapid messages from both windows\n    for (let round = 0; round < 3; round++) {\n      await TestHelpers.runConcurrentActions([\n        () => chatPages[0].sendMessage(rapidMessages[0][round]),\n        () => chatPages[1].sendMessage(rapidMessages[1][round])\n      ]);\n      \n      // Wait for responses\n      await Promise.all([\n        chatPages[0].waitForResponse(),\n        chatPages[1].waitForResponse()\n      ]);\n    }\n    \n    // Verify all messages processed\n    for (let i = 0; i < 2; i++) {\n      for (let j = 0; j < 3; j++) {\n        await expect(chatPages[i].messagesContainer).toContainText(rapidMessages[i][j]);\n      }\n    }\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should handle concurrent image uploads', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 2);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Setup mock API\n    for (const page of pages) {\n      await TestHelpers.setupMockAPI(page, 'success');\n    }\n    \n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Prepare test images\n    const testImages = [\n      await TestHelpers.createTestImage(100, 100),\n      await TestHelpers.createTestImage(150, 150)\n    ];\n    \n    // Upload images concurrently\n    await TestHelpers.runConcurrentActions([\n      async () => {\n        const fileChooserPromise = pages[0].waitForEvent('filechooser');\n        await chatPages[0].imageUploadButton.click();\n        const fileChooser = await fileChooserPromise;\n        await fileChooser.setFiles([{\n          name: 'concurrent-image-1.png',\n          mimeType: 'image/png',\n          buffer: testImages[0]\n        }]);\n        await chatPages[0].sendMessage('Analyze image 1');\n      },\n      async () => {\n        const fileChooserPromise = pages[1].waitForEvent('filechooser');\n        await chatPages[1].imageUploadButton.click();\n        const fileChooser = await fileChooserPromise;\n        await fileChooser.setFiles([{\n          name: 'concurrent-image-2.png',\n          mimeType: 'image/png',\n          buffer: testImages[1]\n        }]);\n        await chatPages[1].sendMessage('Analyze image 2');\n      }\n    ]);\n    \n    // Verify both uploads processed\n    await Promise.all([\n      chatPages[0].waitForResponse(),\n      chatPages[1].waitForResponse()\n    ]);\n    \n    await expect(chatPages[0].messagesContainer).toContainText('concurrent-image-1.png');\n    await expect(chatPages[1].messagesContainer).toContainText('concurrent-image-2.png');\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should maintain connection health across multiple sessions', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 4);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Setup mock API\n    for (const page of pages) {\n      await TestHelpers.setupMockAPI(page, 'success');\n    }\n    \n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Send messages and monitor connection health\n    for (let round = 0; round < 3; round++) {\n      await TestHelpers.runConcurrentActions(\n        chatPages.map((chatPage, index) => \n          () => chatPage.sendMessage(`Round ${round + 1} from session ${index + 1}`)\n        )\n      );\n      \n      // Check connection health for all sessions\n      const healthChecks = await Promise.all(\n        chatPages.map(chatPage => chatPage.getConnectionHealth())\n      );\n      \n      healthChecks.forEach((health, index) => {\n        expect(health.hasError).toBeFalsy();\n      });\n      \n      await Promise.all(chatPages.map(chatPage => chatPage.waitForResponse()));\n    }\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should handle mixed connection states', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 3);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Setup different API responses for each page\n    await TestHelpers.setupMockAPI(pages[0], 'success');  // Success\n    await TestHelpers.setupMockAPI(pages[1], 'error');    // Error\n    await TestHelpers.setupMockAPI(pages[2], 'slow');     // Slow\n    \n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Send messages concurrently with different expected outcomes\n    await TestHelpers.runConcurrentActions([\n      () => chatPages[0].sendMessage('Success message'),\n      () => chatPages[1].sendMessage('Error message'),\n      () => chatPages[2].sendMessage('Slow message')\n    ]);\n    \n    // Verify different outcomes\n    await chatPages[0].waitForConnectionStatus('connected');\n    await chatPages[1].waitForConnectionStatus('error');\n    \n    // For slow response, either connecting or eventual success\n    const slowHealth = await chatPages[2].getConnectionHealth();\n    expect(slowHealth.statusText?.toLowerCase()).toMatch(/connecting|connected/);\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should prevent session interference', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 2);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Setup different mock responses to verify independence\n    await pages[0].route('**/api/claude-code/streaming-chat', route => {\n      route.fulfill({\n        status: 200,\n        contentType: 'application/json',\n        body: JSON.stringify({\n          success: true,\n          responses: [{ content: 'Response from session A', timestamp: new Date().toISOString() }]\n        })\n      });\n    });\n    \n    await pages[1].route('**/api/claude-code/streaming-chat', route => {\n      route.fulfill({\n        status: 200,\n        contentType: 'application/json',\n        body: JSON.stringify({\n          success: true,\n          responses: [{ content: 'Response from session B', timestamp: new Date().toISOString() }]\n        })\n      });\n    });\n    \n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Send messages\n    await chatPages[0].sendMessage('Message to session A');\n    await chatPages[1].sendMessage('Message to session B');\n    \n    // Wait for responses\n    await Promise.all([\n      chatPages[0].waitForResponse(),\n      chatPages[1].waitForResponse()\n    ]);\n    \n    // Verify session isolation\n    await expect(chatPages[0].messagesContainer).toContainText('Response from session A');\n    await expect(chatPages[0].messagesContainer).not.toContainText('Response from session B');\n    \n    await expect(chatPages[1].messagesContainer).toContainText('Response from session B');\n    await expect(chatPages[1].messagesContainer).not.toContainText('Response from session A');\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n\n  test('should handle resource sharing efficiently', async ({ browser }) => {\n    const contexts = await TestHelpers.createMultipleContexts(browser, 5);\n    const pages = await Promise.all(contexts.map(ctx => ctx.newPage()));\n    \n    const chatPages = pages.map(page => new AviChatPage(page));\n    const postingPages = pages.map(page => new PostingInterfacePage(page));\n    \n    // Monitor network activity\n    const networkActivities = await Promise.all(\n      pages.map(page => TestHelpers.captureNetworkActivity(page))\n    );\n    \n    // Setup API\n    for (const page of pages) {\n      await TestHelpers.setupMockAPI(page, 'success');\n    }\n    \n    await Promise.all(postingPages.map(async (postingPage) => {\n      await postingPage.navigateToPostingInterface();\n      await postingPage.switchToTab('avi');\n    }));\n    \n    // Send load test\n    const loadTestActions = chatPages.map((chatPage, index) => \n      () => chatPage.sendMessage(`Load test message ${index + 1}`)\n    );\n    \n    const startTime = Date.now();\n    await TestHelpers.runConcurrentActions(loadTestActions);\n    \n    await Promise.all(chatPages.map(chatPage => chatPage.waitForResponse()));\n    const totalDuration = Date.now() - startTime;\n    \n    // Verify reasonable performance under load\n    expect(totalDuration).toBeLessThan(15000); // Should complete within 15 seconds\n    \n    // Verify all sessions completed successfully\n    for (const chatPage of chatPages) {\n      const health = await chatPage.getConnectionHealth();\n      expect(health.isConnected).toBeTruthy();\n    }\n    \n    await Promise.all(contexts.map(ctx => ctx.close()));\n  });\n});"}, {"old_string": "", "new_string": ""}]